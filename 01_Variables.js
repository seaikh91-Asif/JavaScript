/*
 Note: prefer not to use var 
       always try to prefer const and let for block scope and functional scope purpose.
*/

let AccountName = "first accout"

const accountAddress = "permanet address"

var accountMail = "accout@google.com"

let accoutDetails = "eCommers website"

console.table([AccountName, accountAddress, accountMail, accoutDetails])

